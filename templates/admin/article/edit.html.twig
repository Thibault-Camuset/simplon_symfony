{% extends 'admin/layout.html.twig' %}

{% block title %}Edit Article{% endblock %}

{% block content %}
    <div>
        <h1 class="d-flex justify-content-center">Edit Article</h1>

        <div class="d-flex justify-content-center">{{ include('admin/article/_form.html.twig', {'button_label': 'Update'}) }}</div>

        <div class="primary d-flex justify-content-center"><a href="{{ path('article_index') }}" class="btn btn-primary">Retourner à la liste</a></div>

        <div class="d-flex justify-content-center">{{ include('admin/article/_delete_form.html.twig') }}</div>
    </div>
{% endblock %}
